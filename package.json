{
  "name": "discord-injections",
  "version": "1.0.0",
  "description": "Injects CSS and JS into the discord client, and piggybacks the websocket for later use.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install": "node install.js install",
    "reinstall": "node install.js reinstall",
    "uninstall": "node install.js uninstall"
  },
  "author": "stupid cat",
  "license": "ISC",
  "dependencies": {
    "asar": "^0.13.0",
    "ps-node": "^0.1.5"
  }
}